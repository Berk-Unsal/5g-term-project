# Rendered Manifests

This directory stores rendered YAML from the Helm chart.

Generate manifests with:

```bash
bash scripts/render_manifests.sh
```

Default output file:

- `manifests/all-in-one.yaml`
